{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "install": "bash -lc 'export PATH=\"$HOME/.local/bin:$PATH\"; if ! command -v poetry >/dev/null 2>&1; then python3 -m pip install --user poetry; fi; poetry install -E all'"
}
